# Frontend

## Configuração

Copie `.env.example` para `.env` e ajuste a URL da API:

```bash
cp .env.example .env
```

O Vite disponibiliza no frontend apenas variáveis com o prefixo `VITE_`.

## Instalação e execução

```bash
npm install
npm run dev
```
